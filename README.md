# MSCS532 Assignment 1

## Insertion Sort in Python

This project implements the Insertion Sort algorithm in Python. The program sorts an array in monotonically decreasing order, meaning the largest values come first.

## File Included

- `insertion_sort.py`: Python program for descending insertion sort.

## How to Run

Make sure Python 3.8 or higher is installed.

Run the program using:

```bash
python insertion_sort.py